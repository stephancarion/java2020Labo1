package be.technifutur.java2020.gestionStage.stage;

import java.time.format.DateTimeFormatter;
import java.util.List;

public class StageVue {/*

    public static List<Integer> afficher(List<StageModel> models){
        List<Integer> numeroStageList = null;
        String affiche ="";

        if (models.size() == 0){
            affiche += "-----------------------------------------------------------\n";
            affiche += "---------------------- Pas de stages ----------------------\n";
            affiche += "-----------------------------------------------------------\n";
        }else {
            // Titre
            affiche += "----------------------------------------------------------\n";
            affiche += "-------------------- Liste des stages --------------------\n";
            affiche += "----------------------------------------------------------\n";

            // Liste des stages
            int cpt=0;
            for (StageModel model:models) {
                numeroStageList.add(++cpt);
                String nom = model.getNom();
                String debut = model.getDebut().format(DateTimeFormatter.ofPattern());
                affiche +=
            }
        }

        System.out.println(affiche);
        return numeroStageList;
    }*/
}
